{
  "name": "webapp",
  "version": "1.0.0",
  "description": "DevSecOps webapp - TP2 Cloud",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest --coverage --coverageReporters=lcov --coverageReporters=text-summary"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
